#!/bin/bash
set -euo pipefail

if systemctl is-active --quiet showroom-kiosk.service; then
  exit 0
fi

logger -t showroom-kiosk-retry "resetting kiosk rate limit for a periodic recovery attempt"
systemctl reset-failed showroom-kiosk.service || true
systemctl start showroom-kiosk.service
